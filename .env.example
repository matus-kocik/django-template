# Django settings
SECRET_KEY=your-secret-key               # Secret key for the Django application
DEBUG=True                               # Enable debug mode (True for development, False for production)

# Allowed hosts
ALLOWED_HOSTS=localhost,127.0.0.1        # Comma-separated list of allowed hosts

# Database settings
# Uncomment the database you want to use and comment the other

# SQLite settings (default for development)
DB_ENGINE=django.db.backends.sqlite3     # SQLite engine
DB_NAME=db.sqlite3                       # SQLite database file path

# PostgreSQL settings (uncomment to use)
# DB_ENGINE=django.db.backends.postgresql  # PostgreSQL engine
# DB_NAME=your-database-name               # PostgreSQL database name
# DB_USER=your-database-user               # PostgreSQL username
# DB_PASSWORD=your-database-password       # PostgreSQL password
# DB_HOST=localhost                        # Host where PostgreSQL is running
# DB_PORT=5432                             # PostgreSQL port (default 5432)

# Email settings
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend   # Email backend (e.g., SMTP, console)
EMAIL_HOST=smtp.example.com             # SMTP server hostname
EMAIL_PORT=587                           # SMTP server port (e.g., 587 for TLS)
EMAIL_USE_TLS=True                       # Use TLS for secure connection
EMAIL_HOST_USER=your-email@example.com   # SMTP username (email address)
EMAIL_HOST_PASSWORD=your-email-password  # SMTP password

# Static and media files
STATIC_URL=/static/                      # URL prefix for static files
STATIC_ROOT=staticfiles/                 # Directory where static files are collected
MEDIA_URL=/media/                        # URL prefix for media files
MEDIA_ROOT=media/                        # Directory where media files are stored
